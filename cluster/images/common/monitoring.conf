canton {
  monitoring {
    logging {
      api {
        max-method-length = 100
      }
    }

    metrics {
      report-jvm-metrics = yes
      reporters = [{
        type = prometheus
        address = "0.0.0.0"
        port = 10013
      }]
    }

  }
}
