### Validator App @ Splitwise

server {
    listen       6203;
    server_name  localhost;

    location / {
       proxy_pass http://splitwise-validator-app:6203;
       proxy_http_version 1.1;
    }
}
