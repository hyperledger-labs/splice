### Canton Domain

server {
    listen       5108 http2;
    server_name  localhost;

    location / {
       grpc_pass grpc://splitwise-domain:5008;
    }
}

server {
    listen       5109 http2;
    server_name  localhost;

    location / {
       grpc_pass grpc://splitwise-domain:5009;
    }
}

server {
    listen       10413;
    server_name  localhost;

    location / {
       proxy_pass http://splitwise-domain:10013;
    }
}
