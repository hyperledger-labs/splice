### Validator App @ Splitwell

server {
    listen       6203;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       proxy_pass http://splitwell-validator-app.splitwell.svc.cluster.local:6203;
    }
}
