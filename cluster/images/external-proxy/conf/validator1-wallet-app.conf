### Wallet App @ Validator1

server {
    listen       5104 http2;
    server_name  localhost;

    location / {
       grpc_pass grpc://wallet-app.validator1:5004;
    }
}

server {
    listen       6004;
    server_name  localhost;

    location / {
       proxy_pass http://wallet-app.validator1:6004;
       proxy_http_version 1.1;
    }
}
