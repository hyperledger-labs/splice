### Wallet App @ Validator1

server {
    listen       5104 http2;
    server_name  localhost;

    location / {
       grpc_pass grpc://validator1-wallet-app:5104;
    }
}

server {
    listen       6104;
    server_name  localhost;

    location / {
       proxy_pass http://validator1-wallet-app:6104;
       proxy_http_version 1.1;
    }
}
