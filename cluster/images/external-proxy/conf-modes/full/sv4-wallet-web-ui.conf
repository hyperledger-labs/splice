### Wallet Web UI @ SV4

server {
    listen       80;
    listen  [::]:80;

    server_name  wallet.sv-4.svc.*;

    location ~ ^/(api/v0/validator/)(.*) {
       resolver kube-dns.kube-system.svc.cluster.local;
       proxy_pass http://validator-app.sv-4.svc.cluster.local:5003/$2$is_args$args;
    }

    location / {
        resolver kube-dns.kube-system.svc.cluster.local;
        proxy_pass http://wallet-web-ui.sv-4.svc.cluster.local;
    }
}
