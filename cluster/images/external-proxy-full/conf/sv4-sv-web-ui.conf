### SV Web UI @ SV 4

server {
    listen       80;
    listen  [::]:80;

    server_name  sv.sv-4.svc.*;

    location ~ ^/(api/v0/sv/)(.*) {
       resolver kube-dns.kube-system.svc.cluster.local;
       proxy_http_version 1.1;
       proxy_pass http://sv-app.sv-4.svc.cluster.local:5014/$2$is_args$args;
    }

    location / {
        resolver kube-dns.kube-system.svc.cluster.local;
        proxy_http_version 1.1;
        proxy_pass http://sv-web-ui.sv-4.svc.cluster.local;
    }
}
