### Wallet Web UI @ SV3

server {
    listen       80;
    listen  [::]:80;

    server_name  wallet.sv-3.svc.*;

    location ~ ^/(api/v0/validator/)(.*) {
       resolver kube-dns.kube-system.svc.cluster.local;
       proxy_http_version 1.1;
       proxy_pass http://validator-app.sv-3.svc.cluster.local:5003/$2$is_args$args;
    }

    location / {
        resolver kube-dns.kube-system.svc.cluster.local;
        proxy_http_version 1.1;
        proxy_pass http://wallet-web-ui.sv-3.svc.cluster.local;
    }
}
