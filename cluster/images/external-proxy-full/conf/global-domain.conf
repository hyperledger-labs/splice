### Canton Domain

server {
    listen       5008 http2;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       grpc_pass grpc://global-domain.svc.svc.cluster.local:5008;
    }
}

server {
    listen       5009 http2;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       grpc_pass grpc://global-domain.svc.svc.cluster.local:5009;
    }
}

server {
    listen       10313;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       proxy_pass http://global-domain.svc.svc.cluster.local:10013;
    }
}
