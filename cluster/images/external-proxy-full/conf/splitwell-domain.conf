### Canton Domain

server {
    listen       5108 http2;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       grpc_pass grpc://domain.splitwell.svc.cluster.local:5008;
    }
}

server {
    listen       5109 http2;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       grpc_pass grpc://domain.splitwell.svc.cluster.local:5009;
    }
}

server {
    listen       10413;
    server_name  localhost;

    location / {
       resolver kube-dns.kube-system.svc.cluster.local;
       proxy_http_version 1.1;
       proxy_pass http://domain.splitwell.svc.cluster.local:10013;
    }
}
