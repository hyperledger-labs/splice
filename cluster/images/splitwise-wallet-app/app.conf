_client_credentials_auth_config {
  type = "client-credentials"
  well-known-config-url = ${?CN_APP_SPLITWISE_WALLET_LEDGER_API_AUTH_URL}
  client-id = ${?CN_APP_SPLITWISE_WALLET_LEDGER_API_AUTH_CLIENT_ID}
  client-secret = ${?CN_APP_SPLITWISE_WALLET_LEDGER_API_AUTH_CLIENT_SECRET}
  # TODO(#1837) use actual audience of the target participant
  audience = "https://canton.network.global"
}

_participant_remote {
  admin-api = {
    address = "splitwise-participant"
    port = 5002
  }

  ledger-api = {
    client-config = {
      address = "splitwise-participant"
      port = 5001
    }
    auth-config = ${_client_credentials_auth_config}
  }
}

_validator_remote {
  admin-api.port = 5203
  admin-api.address = "splitwise-validator-app"
  admin-api.url = "http://splitwise-validator-app:6203"
}

_scan_remote {
  admin-api.port = 5012
  admin-api.address = "scan-app"
}

canton {
  wallet-app-backends {
    splitwise_wallet_backend {
      storage.type = memory
      admin-api = {
        address = "0.0.0.0"
        port = 5204
      }
      remote-participant = ${_participant_remote}
      remote-scan = ${_scan_remote}
      validator = ${_validator_remote}
      service-user = ${?CN_APP_SPLITWISE_WALLET_LEDGER_API_AUTH_USER_NAME}
      validator-auth = ${_client_credentials_auth_config}
      auth {
        algorithm = "rs-256"
        audience = "https://canton.network.global"
        jwks-url = "https://canton-network-dev.us.auth0.com/.well-known/jwks.json"
      }
      domains {
        global = "global"
      }
    }
  }
  features.enable-testing-commands = yes
}
