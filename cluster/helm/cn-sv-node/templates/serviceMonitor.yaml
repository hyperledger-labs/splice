{{- $appIdentifier := include "domainMigrationIdOrDefaultValue" (list .Values "sv-app") }}
{{- if .Values.metrics.enable }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    release: {{ .Values.metrics.release }}
  name: {{ $appIdentifier }}
  namespace: {{ .Release.Namespace }}
spec:
  endpoints:

    - port: metrics
      interval: {{ .Values.metrics.interval }}

  selector:
    matchLabels:
      app: {{ $appIdentifier }}

  namespaceSelector:
    matchNames:
      - {{ .Release.Namespace }}

{{- end }}
