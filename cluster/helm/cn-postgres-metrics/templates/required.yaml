{{ $_ := required ".Release.Name is required." .Release.Name }}
{{ $_ := required ".Release.Namespace is required." .Release.Namespace }}
{{ $_ := required ".Values.postgres.hostname is required." .Values.postgres.hostname }}
{{ $_ := required ".Values.postgres.port is required." .Values.postgres.port }}
{{ $_ := required ".Values.postgres.database is required." .Values.postgres.database }}
{{ $_ := required ".Values.postgresSecretName is required" .Values.postgresSecretName }}
