---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-secrets"
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  postgresPassword: {{ .Values.postgres.password | trim |  b64enc }}
