apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: participant
  name: participant
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: participant
  template:
    metadata:
      labels:
        app: participant
    spec:
      containers:
      - name: participant
        image: "{{ .Values.imageRepo }}/canton-participant:{{ .Values.cluster.imageTag }}"
        env:
        - name: JAVA_TOOL_OPTIONS
          value: -Xms24576M -Xmx24576M -Dscala.concurrent.context.minThreads=4
        - name: CN_APP_VALIDATOR_LEDGER_API_AUTH_USER_NAME
          valueFrom:
            secretKeyRef:
              key: ledger-api-user
              name: cn-app-validator-ledger-api-auth
              optional: false
        - name: CANTON_PARTICIPANT_POSTGRES_SERVER
          value: {{ .Values.postgres }}
        - name: CANTON_PARTICIPANT_POSTGRES_SCHEMA
          value: val1_participant
        - name: CANTON_PARTICIPANT_USERS
          value: '[{"actAs": [{"fromUser": "self"}], "admin": true, "name": {"env":
            "CN_APP_VALIDATOR_LEDGER_API_AUTH_USER_NAME"}, "primaryParty": {"allocate":
            "validator1_validator_service_user"}, "readAs": [ ]}]'
        - name: CANTON_PARTICIPANT_EXTRA_DOMAINS
          value: '[{"alias": "splitwell", "url": "http://domain.splitwell:5008"}]'
        imagePullPolicy: Always
        ports:
        - containerPort: 5002
          name: val1-adm-api
          protocol: TCP
        - containerPort: 5001
          name: val1-lg-api
          protocol: TCP
        - containerPort: 10013
          name: val1-metrics
          protocol: TCP
        resources:
          limits:
            memory: 32Gi
          requests:
            cpu: "1"
            memory: 32Gi
      - name: envoy-proxy-val1-lg-api
        image: "{{ .Values.imageRepo }}/envoy-proxy:{{ .Values.cluster.imageTag }}"
        env:
        - name: GRPC_ADDRESS
          value: 127.0.0.1
        - name: GRPC_PORT
          value: "5001"
        - name: GRPC_WEB_PORT
          value: "6001"
        imagePullPolicy: Always
        ports:
        - containerPort: 6001
          name: val1-lg-api-gw
          protocol: TCP
        resources:
          limits:
            memory: 256Mi
          requests:
            memory: 256Mi
      - name: envoy-proxy-val1-adm-api
        image: "{{ .Values.imageRepo }}/envoy-proxy:{{ .Values.cluster.imageTag }}"
        env:
        - name: GRPC_ADDRESS
          value: 127.0.0.1
        - name: GRPC_PORT
          value: "5002"
        - name: GRPC_WEB_PORT
          value: "6002"
        imagePullPolicy: Always
        ports:
        - containerPort: 6002
          name: val1-adm-api-gw
          protocol: TCP
        resources:
          limits:
            memory: 256Mi
          requests:
            memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: participant
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: participant
  ports:
  - name: val1-adm-api
    port: 5002
    protocol: TCP
    targetPort: 5002
  - name: val1-lg-api
    port: 5001
    protocol: TCP
    targetPort: 5001
  - name: val1-metrics
    port: 10013
    protocol: TCP
    targetPort: 10013
  - name: val1-lg-api-gw
    port: 6001
    protocol: TCP
    targetPort: 6001
  - name: val1-adm-api-gw
    port: 6002
    protocol: TCP
    targetPort: 6002
