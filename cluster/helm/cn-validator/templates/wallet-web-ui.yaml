{{- $walletWebUiLabel := include "appLabel" (list .Values "wallet-web-ui") }}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ $walletWebUiLabel }}
  name: {{ $walletWebUiLabel }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $walletWebUiLabel }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: {{ $walletWebUiLabel }}
    spec:
      containers:
      - env:
        - name: CN_APP_WALLET_UI_AUTH_CLIENT_ID
          valueFrom:
            secretKeyRef:
              key: client-id
              name: cn-app-wallet-ui-auth
              optional: false
        - name: CN_APP_WALLET_UI_AUTH_URL
          valueFrom:
            secretKeyRef:
              key: url
              name: cn-app-wallet-ui-auth
              optional: false
        - name: CN_APP_WALLET_UI_AUTH_AUDIENCE
          value: {{ (.Values.auth).audience | default "https://canton.network.global" | quote }}
        - name: CN_APP_WALLET_UI_CLUSTER
          value: {{ .Values.clusterUrl | quote }}
        image: "{{ .Values.imageRepo }}/wallet-web-ui:{{ (.Values.cluster).imageTag }}"
        imagePullPolicy: Always
        name: wallet-web-ui
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          limits:
            cpu: 1
            memory: 1536Mi
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $walletWebUiLabel }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ $walletWebUiLabel }}
  ports:
  - name: http
    port: 80
    protocol: TCP
