{{- $cnsWebUiLabel := "cns-web-ui" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ $cnsWebUiLabel }}
    migration: {{ .Values.migration.id | quote }}
  name: {{ $cnsWebUiLabel }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ $cnsWebUiLabel }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: {{ $cnsWebUiLabel }}
        migration: {{ .Values.migration.id | quote }}
    spec:
      containers:
      - name: cns-web-ui
        image: "{{ .Values.imageRepo }}/cns-web-ui:{{ .Chart.AppVersion }}"
        imagePullPolicy: Always
        env:
        - name: CN_APP_CNS_UI_AUTH_CLIENT_ID
          valueFrom:
            secretKeyRef:
              key: client-id
              name: cn-app-cns-ui-auth
              optional: false
        - name: CN_APP_CNS_UI_AUTH_URL
          valueFrom:
            secretKeyRef:
              key: url
              name: cn-app-cns-ui-auth
              optional: false
        - name: CN_APP_CNS_UI_AUTH_AUDIENCE
          value: {{ (.Values.auth).audience | default "https://canton.network.global" | quote }}
        - name: CN_APP_CNS_UI_CLUSTER
          value: {{ .Values.clusterUrl | quote }}
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        resources:
          requests:
            cpu: 0.1
            memory: 240Mi
          limits:
            cpu: 1
            memory: 1536Mi
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $cnsWebUiLabel }}
  namespace: {{ .Release.Namespace }}
  labels:
    migration: {{ .Values.migration.id | quote }}
spec:
  selector:
    app: {{ $cnsWebUiLabel }}
  ports:
  - name: http
    port: 80
    protocol: TCP
