apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: directory-web-ui
  name: directory-web-ui
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: directory-web-ui
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: directory-web-ui
    spec:
      containers:
      - name: directory-web-ui
        image: "{{ .Values.imageRepo }}/directory-web-ui:{{ (.Values.cluster).imageTag }}"
        imagePullPolicy: Always
        env:
        - name: JAVA_TOOL_OPTIONS
          value: {{ .Values.defaultJvmOptions }}
        - name: CN_APP_DIRECTORY_UI_AUTH_CLIENT_ID
          valueFrom:
            secretKeyRef:
              key: client-id
              name: cn-app-directory-ui-auth
              optional: false
        - name: CN_APP_DIRECTORY_UI_AUTH_URL
          valueFrom:
            secretKeyRef:
              key: url
              name: cn-app-directory-ui-auth
              optional: false
        ports:
        - containerPort: 80
          name: http
          protocol: TCP
        {{- with .Values.resources }}
        resources: {{- toYaml . | nindent 10 }}
        {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: directory-web-ui
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: directory-web-ui
  ports:
  - name: http
    port: 80
    protocol: TCP
