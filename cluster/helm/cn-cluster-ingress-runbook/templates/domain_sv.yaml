{{- if and .Values.withSvIngress ((.Values.ingress).globalDomain).globalDomainId }}
  {{- if .Values.ingress.sequencer }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: sequencer-{{ (.Values.cluster).svNamespace }}-domain-{{ .Values.ingress.globalDomain.globalDomainId }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "sequencer-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.{{ (.Values.cluster).hostPrefix }}{{ (.Values.cluster).hostname }}"
  gateways:
    - cluster-ingress/cn-http-gateway
  http:
    - match:
        - port: 443
      route:
        - destination:
            port:
              number: 5008
            host: global-domain-{{ .Values.ingress.globalDomain.globalDomainId }}-sequencer.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
---
  {{- end }}
  {{- if .Values.ingress.sv }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: sv-sv-{{ (.Values.cluster).svNamespace }}-domain-{{ .Values.ingress.globalDomain.globalDomainId }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "sv-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.{{ (.Values.cluster).hostPrefix }}{{ (.Values.cluster).hostname }}"
  gateways:
    - cluster-ingress/cn-http-gateway
  http:
    - match:
        - port: 443
          uri:
            prefix: "/api/sv/"
      route:
        - destination:
            port:
              number: 5014
            host: sv-app-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
    - match:
        - port: 443
      route:
        - destination:
            port:
              number: 80
            host: sv-web-ui-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
---
  {{- end }}
  {{- if .Values.ingress.scan }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: scan-sv-{{ (.Values.cluster).svNamespace }}-domain-{{ .Values.ingress.globalDomain.globalDomainId }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "scan-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.{{ (.Values.cluster).hostPrefix }}{{ (.Values.cluster).hostname }}"
  gateways:
    - cluster-ingress/cn-http-gateway
  http:
    - match:
        - port: 443
          uri:
            prefix: "/api/scan/"
      route:
        - destination:
            port:
              number: 5012
            host: scan-app-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
    - match:
        - port: 443
      route:
        - destination:
            port:
              number: 80
            host: scan-web-ui-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
---
  {{- end }}
  {{- if .Values.ingress.wallet }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: wallet-sv-{{ (.Values.cluster).svNamespace }}-domain-{{ .Values.ingress.globalDomain.globalDomainId }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "wallet-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.{{ (.Values.cluster).hostPrefix }}{{ (.Values.cluster).hostname }}"
  gateways:
    - cluster-ingress/cn-http-gateway
  http:
    - match:
        - port: 443
          uri:
            prefix: "/api/validator/"
      route:
        - destination:
            port:
              number: 5003
            host: validator-app-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
    - match:
        - port: 443
      route:
        - destination:
            port:
              number: 80
            host: wallet-web-ui-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
---
  {{- end }}
  {{- if .Values.ingress.cns }}
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: cns-sv-{{ (.Values.cluster).svNamespace }}-domain-{{ .Values.ingress.globalDomain.globalDomainId }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
    - "cns-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.{{ (.Values.cluster).hostPrefix }}{{ (.Values.cluster).hostname }}"
  gateways:
    - cluster-ingress/cn-http-gateway
  http:
    - match:
        - port: 443
          uri:
            prefix: "/api/validator/"
      route:
        - destination:
            port:
              number: 5003
            host: validator-app-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
    - match:
        - port: 443
      route:
        - destination:
            port:
              number: 80
            host: cns-web-ui-{{ .Values.ingress.globalDomain.globalDomainId }}.{{ (.Values.cluster).svNamespace }}.svc.cluster.local
---
  {{- end }}
{{- end }}
