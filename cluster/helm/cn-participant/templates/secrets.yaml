---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-secrets"
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  cantonParticipantPostgresPassword: {{ .Values.postgresPassword | trim |  b64enc }}
