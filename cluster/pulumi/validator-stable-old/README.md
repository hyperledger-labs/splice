# Validator stable old (Pulumi project)

Deploys the `validator-stable-old` testing validator with custom version.

Deployment typically managed via an [operator](../operator). For manual deployment s.a. `cncluster apply`.
