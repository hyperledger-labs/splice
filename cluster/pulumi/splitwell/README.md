# splitwell (Pulumi project)

Deploys the `splitwell` testing validator as well as the `splitwell` backend app.

Deployment typically managed via an [operator](../operator). For manual deployment s.a. `cncluster apply`.
