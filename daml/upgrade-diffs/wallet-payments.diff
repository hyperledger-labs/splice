diff -ur -x '*~' -x target -x dar -x .daml ../daml/wallet-payments/daml/CN/Wallet/Payment.daml ../daml/wallet-payments-upgrade/daml/CN/Wallet/Payment.daml
--- ../daml/wallet-payments/daml/CN/Wallet/Payment.daml
+++ ../daml/wallet-payments-upgrade/daml/CN/Wallet/Payment.daml
@@ -109,6 +109,7 @@
                       expiresAt = expiresAt
                     receiverFeeRatio = 0.0
                 ]
+              upgrade = Some ()
          result <- exercisePaymentTransfer context transfer
          let [TransferResultLockedCoin lockedCoin] = result.createdCoins
          let round = result.round
@@ -179,6 +180,7 @@
               provider
               inputs = [InputCoin ccsum.coin]
               outputs = map mkReceiverOutput ccReceiverAmounts
+              upgrade = Some ()
         result <- exerciseAppTransfer context transfer
         require "Precomputed fees were exact" (result.summary.senderChangeFee == 0.0)
         let coins = map (\(TransferResultCoin coinCid) -> coinCid) result.createdCoins
diff -ur -x '*~' -x target -x dar -x .daml ../daml/wallet-payments/daml/CN/Wallet/Subscriptions.daml ../daml/wallet-payments-upgrade/daml/CN/Wallet/Subscriptions.daml
--- ../daml/wallet-payments/daml/CN/Wallet/Subscriptions.daml
+++ ../daml/wallet-payments-upgrade/daml/CN/Wallet/Subscriptions.daml
@@ -329,6 +329,7 @@
                expiresAt = expiresAt
              receiverFeeRatio = 0.0
          ]
+       upgrade = Some ()
 
   result <- exercisePaymentTransfer context transfer
   let [TransferResultLockedCoin lockedCoin] = result.createdCoins
@@ -347,6 +348,7 @@
         provider
         inputs = [InputCoin ccsum.coin]
         outputs = [mkTransferOutput receiver targetAmount]
+        upgrade = Some ()
   result <- exerciseAppTransfer transferContext transfer
   require "Precomputed fees were exact" (result.summary.senderChangeFee == 0.0)
   let [TransferResultCoin coinCid] = result.createdCoins
diff -ur -x '*~' -x target -x dar -x .daml ../daml/wallet-payments/daml.yaml ../daml/wallet-payments-upgrade/daml.yaml
--- ../daml/wallet-payments/daml.yaml
+++ ../daml/wallet-payments-upgrade/daml.yaml
@@ -1,14 +1,14 @@
 sdk-version: 2.8.0-snapshot.20231026.12262.0.vb12eb2ad
 name: wallet-payments
 source: daml
-version: 0.1.0
+version: 0.2.0
 dependencies:
   - daml-prim
   - daml-stdlib
   - daml-script
 data-dependencies:
 - ../cn-util/.daml/dist/cn-util-0.1.0.dar
-- ../canton-coin/.daml/dist/canton-coin-0.1.0.dar
+- ../canton-coin-upgrade/.daml/dist/canton-coin-0.2.0.dar
 build-options:
   - --ghc-option=-Wunused-binds
   - --ghc-option=-Wunused-matches
