module CC.Scripts.TestAmuletRulesDisclosure where

import Daml.Script

import DA.Foldable (mapA_)

import CC.Scripts.Util

test : Script ()
test = do
  DefaultAppWithUsers{..} <- setupDefaultAppWithUsers

  -- -- start issuing so the DSO has some amulets to invite users
  runNextIssuance app
  runAmuletDepositBots app

  mapA_ (\_ -> do
    runNextIssuance app
    runAmuletDepositBots app
    pay app alice bob.primaryParty 0.01
    ) [1,2,3,4,5]


  pure ()
