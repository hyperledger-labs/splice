### SVC App

server {
    listen       5005 http2;
    server_name  localhost;

    location / {
       grpc_pass grpc://svc-app:5005;
    }
}

server {
    listen       5012 http2;
    server_name  localhost;

    location / {
       grpc_pass grpc://svc-app:5012;
    }
}
