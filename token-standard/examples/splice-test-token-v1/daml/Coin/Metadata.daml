module Coin.Metadata where

import Splice.Api.Token.MetadataV1 qualified as Meta
import DA.TextMap qualified as TM

testMeta = Meta.Metadata with
  values = TM.fromList [("purpose", "ability to implement token standard API inside Canton Network")]
