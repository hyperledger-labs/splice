sequencer:
  persistence:
    host: sequencer-pg
    secretName: sequencer-pg-secret
  driver:
    type: "cometbft"
    host: "cometbft-cometbft-rpc"
    port: 26657
mediator:
  persistence:
    host: mediator-pg
    secretName: mediator-pg-secret
