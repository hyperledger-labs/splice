canton {
  validator-apps {
    svValidator {
      onboarding = {
        # our onboarding sponsor is SV1
        sv-client.admin-api.port = 5014
        sv-client.admin-api.address = ${NETWORK_APPS_ADDRESS_PROTOCOL}"://"${NETWORK_APPS_ADDRESS}
        # secret must be obtained from SV1 first
        secret = "PLACEHOLDER"
      }
    }
  }
}
