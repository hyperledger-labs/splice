_validator_participant_remote {
  admin-api.port = 5102
  ledger-api.port = 5101
}

_scan_remote {
  admin-api.port = 5012
  admin-api.address = ${NETWORK_APPS_ADDRESS}
}

canton {
  splitwise-apps {
    splitwiseBackend {
      admin-api.address = 0.0.0.0
      admin-api.port = 5113
      provider-user = "splitwise-service-user"
      remote-participant = ${_validator_participant_remote}
      remote-scan = ${_scan_remote}
      domains {
        global = "global"
        # TODO(#2703) Replace by separate splitwise domain.
        splitwise = "global"
      }
    }
  }
}
