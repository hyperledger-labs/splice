canton {
  validator-apps {
    splitwellValidatorApp {
      onboarding = {
        # our onboarding sponsor; in this example this is SV2
        sv-client.admin-api.url = ${NETWORK_APPS_ADDRESS_PROTOCOL}"://sv.sv-2."${NETWORK_APPS_ADDRESS}""
        # secret must be obtained from onboarding sponsor first
        secret = "PLACEHOLDER"
      }
    }
  }
}
