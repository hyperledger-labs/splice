canton.participants.sv2StandaloneParticipant.crypto.provider = kms
canton.participants.sv2StandaloneParticipant.crypto.kms = {
  type = ${KMS_TYPE}
  location-id = ${KMS_LOCATION_ID}
  project-id = ${KMS_PROJECT_ID}
  key-ring-id = ${KMS_KEY_RING_ID}
}
