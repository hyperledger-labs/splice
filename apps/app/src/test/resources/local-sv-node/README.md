This folder contains configuration files required to start a local copy of the SV node
that is deployed to the cluster (see `cluster/images/**/*.conf`).

The local SV node does not use auth for its participant.
