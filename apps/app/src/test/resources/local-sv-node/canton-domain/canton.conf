canton {
  parameters {
    manual-start = no
    non-standard-config = yes
  }

  domains {
    svc_domain {
      storage.type = memory

      public-api {
        address = "0.0.0.0"
        port = 9008
      }

      admin-api {
        address = "0.0.0.0"
        port = 9009
      }
      init.domain-parameters {
        unique-contract-keys = false
        protocol-version = dev
      }
    }
  }
}
