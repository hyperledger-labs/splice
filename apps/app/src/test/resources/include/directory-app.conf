directory-app {
  storage.type = memory
  admin-api.port = 5110
  admin-api.address = 0.0.0.0
  remote-participant = {
    admin-api.port = 5102
    ledger-api.client-config.port = 5101
    ledger-api.auth-config.type = "none"
  }
  remote-scan.admin-api = ${canton.scan-app.admin-api}
  ledger-api-user = ${canton.validator-apps.directoryValidator.ledger-api-user}
}
