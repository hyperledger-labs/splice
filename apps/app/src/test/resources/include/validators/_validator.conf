{
  include required("../scan-client")
  storage.type = memory

  participant-client = {
    ledger-api.auth-config.type = "none"
  }
  auth {
    algorithm = "hs-256-unsafe"
    audience = "https://canton.network.global"
    secret = "test"
  }
  app-instances {
  }
  domains {
    global {
      alias = "global"
      buy-extra-traffic {
        target-throughput = 20000
        min-topup-interval = "10m"
      }
    }
  }
}
