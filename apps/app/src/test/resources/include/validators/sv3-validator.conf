{
  include required("_validator")

  admin-api.port = 5703
  participant-client = {
    admin-api.port = 5702
    ledger-api.client-config.port = 5701
  }
  ledger-api-user = "sv3_validator_user"
  validator-wallet-user = "sv3_validator_wallet_user"

  onboarding = {
    sv-client.admin-api = ${canton.sv-apps.sv1.admin-api}
    sv-client.admin-api.address = "http://127.0.0.1"
    secret = "sv3secret"
  }
}
