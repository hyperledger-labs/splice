{
  include required("_validator")

  admin-api.port = 5203
  remote-participant = {
    admin-api.port = 5202
    ledger-api.client-config.port = 5201
  }
  ledger-api-user = "alice_validator_user"
  validator-wallet-user = "alice_validator_wallet_user"

  onboarding = {
    remote-sv.admin-api {
      port = ${canton.sv-apps.sv2.admin-api.port}
      address = "http://127.0.0.1"
    }
    secret = "alicesecret"
  }
}
