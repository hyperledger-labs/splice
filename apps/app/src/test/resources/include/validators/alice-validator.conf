{
  include required("_validator")

  admin-api.port = 5203
  remote-participant = {
    admin-api.port = 5202
    ledger-api.client-config.port = 5201
  }
  ledger-api-user = "alice_validator_user"
  validator-wallet-user = "alice_validator_wallet_user"
  wallet-service-user = "alice_wallet_service"

  onboarding = {
    remote-sv.admin-api.port = ${canton.sv-apps.sv2.admin-api.port}
    # 6114 is the port of SV2 bumped by 1000
    # TODO(#2019) use the original port here, via HOCON substitution
    remote-sv.admin-api.url = "http://127.0.0.1:6114"
    secret = "alicesecret"
  }
}
