{
  include required("_validator")

  admin-api.port = 5603
  participant-client = {
    admin-api.port = 5602
    ledger-api.client-config.port = 5601
  }
  ledger-api-user = "sv2_validator_user"
  validator-wallet-user = "sv2_validator_wallet_user"

  onboarding = {
    sv-client.admin-api.url = "http://127.0.0.1:"${canton.sv-apps.sv1.admin-api.port}
    secret = "sv2secret"
  }
}
