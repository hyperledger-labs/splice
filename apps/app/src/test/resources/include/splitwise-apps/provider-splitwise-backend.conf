providerSplitwiseBackend {
  storage.type = memory
  admin-api.address = 0.0.0.0
  admin-api.port = 5113
  remote-scan.admin-api = ${canton.scan-app.admin-api}
  remote-scan.admin-api.url = "http://127.0.0.1:7012"
  remote-participant = ${canton.validator-apps.splitwiseValidator.remote-participant}
  provider-user = "splitwise_provider"
  domains {
    global = "global"
    # TODO (M3-83) Change this to the private splitwise domain.
    splitwise = "global"
  }
}
