bobSplitwise {
  storage.type = memory
  admin-api.port = 5313
  remote-scan.admin-api = ${canton.scan-app.admin-api}
  remote-participant = ${canton.validator-apps.bobValidator.remote-participant}
  daml-user = ${canton.wallet-apps.bobWallet.daml-user}
}
