scan-app {
  storage.type = memory
  admin-api.address = 0.0.0.0
  admin-api.port = 5012
  participant-client = ${canton.svc-app.participant-client}
  svc-user = ${canton.svc-app.ledger-api-user}
  domains {
    global {
      alias = "global"
    }
  }
}
