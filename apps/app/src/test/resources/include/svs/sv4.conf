sv4 {
  storage.type = memory
  admin-api.port = 5017
  remote-participant = {
    admin-api.port = 5002
    ledger-api.client-config.port = 5001
    ledger-api.auth-config.type = "none" # we override this via config transform
  }
  daml-user = "svc_shared_service_user"
}
