{
  include required("_sv")

  admin-api.port = 5014
  ledger-api-user = "sv1"
  validator-ledger-api-user = "sv1_validator_user"
  sv-party-hint = "canton-foundation-1"

  local-domain-node {
    sequencer = {
      admin-api.port = 5009
      internal-api.address = "localhost"
      internal-api.port = 5008
      external-public-api-url = "http://localhost:5008"
    }
    mediator.admin-api.port = 5007
  }

  expected-validator-onboardings = [
    { secret = "alicesecret" },
    { secret = "bobsecret" },
    { secret = "splitwellsecret" }
  ]

  approved-sv-identities = [
    { include required("_sv2-id") }
    { include required("_sv3-id") },
    { include required("_sv4-id") }
  ]

  onboarding = {
    type = "found-collective"
    name = "Canton-Foundation-1"
    is-dev-net = true
  }
}
