{
  include required("_sv")

  admin-api.port = 5014
  ledger-api-user = "sv1"

  expected-onboardings = [ { secret = "directorysecret" }, { secret = "sv1secret" } ]
  approved-sv-identities = [
    { include required("_sv4-id") },
    { include required("_sv5-id") }
  ]

  onboarding = {
    type = "found-collective"
    name = ${ledger-api-user}
  }
}
