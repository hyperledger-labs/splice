aliceDirectory {
  daml-user = ${canton.remote-wallet-apps.aliceRemoteWallet.daml-user}
  ledger-api = ${canton.validator-apps.aliceValidator.remote-participant.ledger-api}
  admin-api.port = ${canton.directory-apps.directoryBackend.admin-api.port}
}
