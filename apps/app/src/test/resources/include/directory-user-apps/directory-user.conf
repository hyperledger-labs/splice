directoryUser {
  storage.type = memory
  admin-api.port = 5111
  daml-user = ${canton.wallet-apps.aliceWallet.daml-user}
  remote-participant = ${canton.validator-apps.aliceValidator.remote-participant}
  remote-directory-provider = {
    admin-api.port = ${canton.directory-provider-apps.directoryProvider.admin-api.port}
    remote-participant = ${canton.directory-provider-apps.directoryProvider.remote-participant}
  }
}
