providerSplitwellBackend {
  include required("../remote-scan")
  storage.type = memory
  admin-api.address = 0.0.0.0
  admin-api.port = 5113
  remote-participant = ${canton.validator-apps.splitwellValidator.remote-participant}
  provider-user = "splitwell_provider"
  domains {
    global = "global"
    splitwell = {
      preferred = "splitwell"
      others = []
    }
  }
}
