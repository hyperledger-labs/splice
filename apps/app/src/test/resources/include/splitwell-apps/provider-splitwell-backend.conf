providerSplitwellBackend {
  storage.type = memory
  admin-api.address = 0.0.0.0
  admin-api.port = 5113
  remote-scan.admin-api = ${canton.scan-app.admin-api}
  remote-scan.admin-api.url = "http://127.0.0.1:7012"
  remote-participant = ${canton.validator-apps.splitwellValidator.remote-participant}
  provider-user = "splitwell_provider"
  domains {
    global = "global"
    # TODO(#2700) Change this to the private splitwell domain.
    splitwell = "global"
  }
}
