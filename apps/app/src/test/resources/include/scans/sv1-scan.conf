{
  include required("_scan")

  participant-client = {
    admin-api.port = 5102
    ledger-api.client-config.port = 5101
  }
  sequencer-admin-client.port = 5109
  # The founder must ingest from the participant begin so it can seed other sv scans with aggregates from round zero and onwards.
  ingest-from-participant-begin = true
  admin-api.port = 5012
  sv-user = "sv1"
}
