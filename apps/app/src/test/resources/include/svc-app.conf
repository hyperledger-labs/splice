svc-app {
  storage.type = memory
  admin-api.port = 5005
  participant-client = {
    admin-api.port = 5002
    ledger-api.client-config.port = 5001
    ledger-api.auth-config.type = "none"
  }
  sv-user = "sv1"
  domains {
    global {
      alias = "global"
    }
  }
}
