svc-app {
  storage.type = memory
  admin-api.port = 5005
  remote-participant = {
    admin-api.port = 5002
    ledger-api.client-config.port = 5001
    ledger-api.auth-config.type = "none"
  }
  ledger-api-user = "svc_shared_service_user"
  domains {
    global = "global"
  }
}
