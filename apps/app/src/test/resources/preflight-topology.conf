canton {
    sv-app-clients {
        sv1.admin-api {
            port = 5014
            address = "https://sv-app.sv-1.svc."${NETWORK_APPS_ADDRESS}
        }

        sv2.admin-api {
            port = 5114
            address = "https://sv-app.sv-2.svc."${NETWORK_APPS_ADDRESS}
        }

        sv3.admin-api {
            port = 5214
            address = "https://sv-app.sv-3.svc."${NETWORK_APPS_ADDRESS}
        }

        sv4.admin-api {
            port = 5314
            address = "https://sv-app.sv-4.svc."${NETWORK_APPS_ADDRESS}
        }
    }

    validator-app-clients {
        validator1 {
            admin-api {
                port = 443
                address = "https://wallet.validator1."${NETWORK_APPS_ADDRESS}"/api/v0/validator"
            }
        }
    }
}
