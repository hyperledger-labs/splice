canton {
  remote-participants {
   remoteParticipant {
      admin-api {
        port = 5002
        address = ${?NETWORK_APPS_ADDRESS}
      }
      ledger-api {
        port = 5001
        address = ${?NETWORK_APPS_ADDRESS}
      }
    }
  }

  remote-validator-apps {
    validator1App {
      admin-api.port = 5103
      admin-api.address = ${?NETWORK_APPS_ADDRESS}
    }
  }

  remote-directory-apps {
    aliceDirectory {
      admin-api.port = 5010
      daml-user = "alice-v1"
      admin-api.address = ${?NETWORK_APPS_ADDRESS}

      ledger-api.client-config.address = ${?NETWORK_APPS_ADDRESS}
      ledger-api.client-config.port = 5101
      ledger-api.auth-config.type = "none"
    }
  }

  wallet-app-clients {
    aliceValidator1Wallet {
      daml-user = "alice-v1"
      admin-api.port = 5104
      admin-api.address = ${?NETWORK_APPS_ADDRESS}
    }

    bobValidator1Wallet {
      daml-user = "bob-v1"
      admin-api.port = 5104
      admin-api.address = ${?NETWORK_APPS_ADDRESS}
    }
  }
}
