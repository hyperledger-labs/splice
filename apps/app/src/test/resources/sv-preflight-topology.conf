include required("preflight-topology.conf")

canton {
    sv-app-clients {
        sv.admin-api {
            url = "https://sv.sv."${NETWORK_APPS_ADDRESS}""
        }
    }
    scan-app-clients {
        svTestScan {
            admin-api {
                url = "https://scan.sv."${NETWORK_APPS_ADDRESS}""
            }
        }
    }
}
