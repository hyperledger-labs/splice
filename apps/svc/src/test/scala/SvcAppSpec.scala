import org.scalatest.funsuite._

// TODO(M1-01): This is just a placeholder that shows where actual unit tests should be added
class SvcAppSpec extends AnyFunSuite {
  test("Hello should start with H") {
    assert("Hello".startsWith("H"))
  }
}
