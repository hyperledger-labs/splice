workflow:
  backup_status_check:
    when:
      equal: [ backup-status-check, << pipeline.parameters.run-job >> ]
    jobs:
      - backup_status_cluster:
          cluster: "devnet"
          channel_notify: C06SMCCBYQH # #team-canton-network-dev-testnet-deployment
          context:
            - slack-context
            - canton-network-vpn
#      - backup_status_cluster:
#          cluster: "testnet"
#          channel_notify: C06SMCCBYQH # #team-canton-network-dev-testnet-deployment
#          context:
#            - slack-context
#            - canton-network-vpn
      - backup_status_cluster:
          cluster: "mainnet"
          channel_notify: C0731SHS3HB # #team-canton-network-mainnet-deployment
          alert_threshold: "4 hours ago"
          context:
            - slack-context
            - canton-network-vpn
