Any triggers defined here will overwrite those on CircleCI after every merge into `main`.

Move a trigger definition file into `../disabled` to have it be automatically deleted on CircleCI after the next merge.
