Drag trigger definition files into this directory to permanently disable them across branch merges into `main`, while still saving the trigger definition for later.

Move a trigger definition file into `../enabled` to have it be automatically recreated on CircleCI after the next merge.
