name: Auto-assign PRs from forks

on:
  pull_request_target:
    types: [opened, reopened, edited]

jobs:
  assign:
    runs-on: ubuntu-24.04
    if: github.event.pull_request.head.repo.full_name != github.event.pull_request.base.repo.full_name
    steps:
      - name: Assign PR from fork
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          github-token: ${{ secrets.GH_CREATE_PRS_TOKEN }}
          script: |
            const pr = context.payload.pull_request;
            // Only add assignees if the PR isn't already assigned to someone
            if (!pr.assignee && !pr.assignees.length) {
              await github.rest.issues.addAssignees({
                issue_number: pr.number,
                owner: pr.base.repo.owner.login,
                repo: pr.base.repo.name,
                assignees: ['isegall-da', 'martinflorian-da', 'ray-roestenburg-da', 'rautenrieth-da'],
              });
            }
