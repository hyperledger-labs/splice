name: contributed workflow
on:
  pull_request:
    types: [ opened, synchronize, reopened ]
    
jobs:
  contributed-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "This was just contributed"
